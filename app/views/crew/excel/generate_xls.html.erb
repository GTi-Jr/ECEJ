<table>
	<thead>
		<% @columns.each do |column| %>
			<th><%= column %></th>
		<% end %>
	</thead>

	<tbody>

		<% @users.each do |user| %>
			<tr>
				<% @columns.each do |column| %>
					<td><%= user.send(column.to_symbol) %></td>
				<% end %>
			</tr>
		<% end %>
	</tbody>
</table>

