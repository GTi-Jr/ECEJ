<table class="highlight">
  <thead>
    <tr>
      <th data-field="name">Nome</th>
      <th data-field="email">Email</th>
      <th data-field="JE">EJ</th>
      <th data-field="Edit">Editar</th>
    </tr>
  </thead>

  <tbody>
    <% if !@users.nil? %>
      <% @users.each do |user| %>
        <tr class="<%= "left-red-border" if !user.active %>">
          <td><%= user.name %></td>
          <td><%= user.email %></td>
          <td><%= user.junior_enterprise %></td>
          <td><%= link_to "Editar", edit_crew_user_path(user) %></td>
          <td><%= link_to "Pago", set_user_payment_path(user.id, 'paid'), method: :patch %></td>
          <td><%= link_to "Em espera", set_user_payment_path(user.id, 'waiting'), method: :patch %></td>
          <td><%= link_to "Não pago", set_user_payment_path(user.id, 'non_paid'), method: :patch %></td>
          <td><%= link_to "Cancelado", set_user_payment_path(user.id, 'cancelled'), method: :patch %></td>
        </tr>
      <% end %> 
    <% end %>
  </tbody>
</table>
